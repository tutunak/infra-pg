[defaults]
roles_path         = roles
inventory          = inventory/hosts.yml
collections_paths  = ~/.ansible/collections
vault_password_file = .vault_pass

# Increase verbosity for change tracking
stdout_callback = yaml

# Speed and safety
retry_files_enabled = False
